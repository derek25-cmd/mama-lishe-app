# Faida Mobile (Flutter)

Placeholder scaffold. Generate full platform folders with:

    flutter create . --org com.faida --project-name faida_mobile

Then run `flutterfire configure` to wire up FCM.
